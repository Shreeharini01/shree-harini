sentence = input()

words = sentence.split()
max_len = 0
longest_word = ""

for word in words:
    if len(word) > max_len:
        max_len = len(word)
        longest_word = word

print( longest_word)
print( max_len)
