def get_winner(votes):
    # Create a dictionary to store the count of votes for each candidate
    vote_counts = {}

    # Count the votes for each candidate
    for vote in votes:
        vote_counts[vote] = vote_counts.get(vote, 0) + 1

    # Find the maximum number of votes
    max_votes = max(vote_counts.values())

    # Create a list of winners (candidates with maximum votes)
    winners = [name for name, count in vote_counts.items() if count == max_votes]

    # Sort the winners lexicographically and return the smallest name
    winners.sort()
    return winners[0]

# Get the number of votes from the user
num_votes = int(input())

# Get the list of votes from the user
votes = []
for _ in range(num_votes):
    vote = input()
    votes.append(vote)

# Call the get_winner function and print the result
winner = get_winner(votes)
print(winner)
