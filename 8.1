def uncommon_words(s1, s2):
    # Split the sentences into words
    words1 = s1.split()
    words2 = s2.split()

    # Create dictionaries to store word counts
    word_count1 = {}
    word_count2 = {}

    # Count the occurrences of each word in s1 and s2
    for word in words1:
        word_count1[word] = word_count1.get(word, 0) + 1
    for word in words2:
        word_count2[word] = word_count2.get(word, 0) + 1

    # Find uncommon words
    uncommon = []
    for word in word_count1:
        if word_count1[word] == 1 and word not in word_count2:
            uncommon.append(word)
    for word in word_count2:
        if word_count2[word] == 1 and word not in word_count1:
            uncommon.append(word)

    return uncommon
# Get input from the user
s1 = input()
s2 = input()

# Call the uncommon_words function and print the result
result = uncommon_words(s1, s2)
for word in result:
    print(word, end=" ")

    
    
